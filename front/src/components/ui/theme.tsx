import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    body: "Times New Roman, serif",  // Para todo el texto
    heading: "Times New Roman, serif",  // Para títulos
  },
});

export default theme;
